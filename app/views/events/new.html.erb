<h1>New event</h1>

<% if user_signed_in? %>
  <%= render "form", event: @event %>
  <br>
<% else %>
  <%= render "login", event: @event %>
  <br>
<% end %>

<br>
<div>
  <%= link_to "Back to events", events_path %>
</div>
